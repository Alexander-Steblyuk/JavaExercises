spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: never
  flyway:
    enabled: true
    locations: classpath:db/migration/schema,classpath:db/migration/test
    migrate-on-start: true
    cleanDisabled: false
  jpa:
    hibernate:
      ddl-auto: none