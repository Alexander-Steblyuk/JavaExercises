spring:
  datasource:
    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: always
      data-locations: data.sql
      schema-locations: db/migration/V1__init_schema.sql
  flyway:
    enabled: false
